const batteryBatches = [4, 5, 3, 4, 4, 6, 5];

fuction totalBatteries(batteryBatches) {
    let totalBatches = 0;

    for (const batteryBatches of totalBatteries) {
        totalBatteries += batteryBatches.value;
    };

    return totalBatteries;
}

console.log(ourReduce(batteryBatches, batchesCounter, 0)); // LOG:31
// Code your solution here